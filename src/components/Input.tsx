import React from 'react';

const Input = () => {
  return (
    <input
      type="text"
      placeholder="Введите текст"
      name="homeworkInput"
      className="input"  
    />
  );
};

export default Input;
